on:
    schedule:
      - cron: '0 */6 * * *'
    workflow_dispatch:
jobs:
  fetch:
    name: Crawl rss and generate static page
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
        name: Check out repository code
      - uses: i-valchev/rss-to-json@latest
        name: Fetch feeds
